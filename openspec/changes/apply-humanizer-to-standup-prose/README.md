# apply-humanizer-to-standup-prose

Vendor a distilled, standup-relevant subset of the `/humanizer` anti-AI-writing patterns into the
`vector-standup-writer` agent so the digest reads human-written by default — always on,
subtractive only, language-agnostic.
