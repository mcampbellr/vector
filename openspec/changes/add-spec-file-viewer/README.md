# add-spec-file-viewer

A read-only `GET /api/file` endpoint plus a `Files` section + in-board preview modal that render a spec's `spec.md` and OpenSpec artifacts as Markdown; paths resolve server-side from committed state (no client path, no disk scan).
