# release-standup-digest

Closeout/release of add-standup-digest: re-embed the web build into the binary, recompile and reinstall the global `vector`, and run an exhaustive manual UAT. No feature re-implementation.
